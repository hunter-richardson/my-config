wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
#wget -O discord.deb "https://discordapp.com/api/download?platform=linux$format=deb" | sudo dpkg -i --

sudo usermod -a -G user,dev,sudo hunter
sudo usermod -a -G user,dev,root hunter-adm
sudo chown root:root /home/hunter*/.local/share/Trash
﻿
